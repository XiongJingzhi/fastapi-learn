# 06. 部署策略 - Deployment Strategies

## 📍 在架构中的位置

**从"停机部署"到"零停机部署"**

```
┌─────────────────────────────────────────────────────────────┐
│          停机部署（传统方式）                                 │
└─────────────────────────────────────────────────────────────┘

部署流程：
    1. 停止旧版本应用
    2. 部署新版本应用
    3. 启动新版本应用
    4. 测试新版本

    问题：
    - 停机时间：5-10 分钟 ❌
    - 用户无法访问 ❌
    - 部署失败？服务中断 ❌

═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│          零停机部署                                           │
└─────────────────────────────────────────────────────────────┘

部署流程：
    1. 新版本与旧版本同时运行
    2. 逐步切换流量到新版本
    3. 确认新版本正常
    4. 移除旧版本

    好处：
    - 零停机时间 ✅
    - 快速回滚 ✅
    - 降低风险 ✅
```

**🎯 你的学习目标**：掌握蓝绿部署、滚动更新、金丝雀发布等部署策略，实现零停机部署。

---

## 🎯 部署策略对比

### 四种常见策略

| 策略 | 停机时间 | 复杂度 | 回滚速度 | 适用场景 |
|------|----------|--------|----------|----------|
| **停机部署** | 高 | 低 | 慢 | 小项目、内部系统 |
| **滚动更新** | 无 | 中 | 快 | 生产环境（推荐） |
| **蓝绿部署** | 无 | 高 | 极快 | 关键业务、需要快速回滚 |
| **金丝雀发布** | 无 | 极高 | 慢 | 大规模、谨慎发布 |

---

## 🔄 滚动更新（Rolling Update）

### 什么是滚动更新？

**生活类比：换轮胎**

```
传统方式（停机）：
    车：停下 ❌
    → 换 4 个轮胎
    → 继续行驶

滚动方式：
    车：继续行驶 ✅
    → 一次换 1 个轮胎
    → 换完第 1 个，继续行驶
    → 换第 2 个...
    → 换完所有轮胎
```

**软件滚动更新**：

```
初始状态：
    Pod A (v1.0) ✅
    Pod B (v1.0) ✅
    Pod C (v1.0) ✅

步骤 1：停止 Pod A
    Pod A (停止) ❌
    Pod B (v1.0) ✅
    Pod C (v1.0) ✅

步骤 2：启动 Pod A (v2.0)
    Pod A (v2.0) ✅
    Pod B (v1.0) ✅
    Pod C (v1.0) ✅

步骤 3：重复步骤 1-2（Pod B, C）

最终状态：
    Pod A (v2.0) ✅
    Pod B (v2.0) ✅
    Pod C (v2.0) ✅
```

---

### Kubernetes 滚动更新

**Deployment 配置**：

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app
spec:
  # 副本数
  replicas: 3

  # 滚动更新策略
  strategy:
    type: RollingUpdate
    rollingUpdate:
      # 最大不可用 Pod 数（1 个）
      maxUnavailable: 1
      # 最大超出 Pod 数（1 个）
      maxSurge: 1

  selector:
    matchLabels:
      app: fastapi

  template:
    metadata:
      labels:
        app: fastapi
    spec:
      containers:
      - name: fastapi
        image: fastapi-app:v1.0
        ports:
        - containerPort: 8000

        # 就绪探针（确保 Pod 准备好才接收流量）
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
```

---

**执行滚动更新**：

```bash
# 更新镜像
kubectl set image deployment/fastapi-app fastapi=fastapi-app:v2.0

# 查看更新状态
kubectl rollout status deployment/fastapi-app

# 查看更新过程
kubectl get pods -w

# 输出：
# NAME                           READY   STATUS
# fastapi-app-7d6f8c9c9d-xv2ts   1/1     Running (v1.0)
# fastapi-app-7d6f8c9c9d-yw3ts   1/1     Running (v1.0)
# fastapi-app-7d6f8c9c9d-zx4ts   1/1     Running (v1.0)
# fastapi-app-8d7f9d9d9e-xv2ts   0/1     Pending (v2.0 新 Pod)
# fastapi-app-8d7f9d9d9e-xv2ts   1/1     Running (v2.0 就绪)
# fastapi-app-7d6f8c9c9d-xv2ts   1/1     Terminating (v1.0 旧 Pod)
# fastapi-app-7d6f8c9c9d-yw3ts   1/1     Terminating (v1.0)
# ...
```

---

## 🔵 蓝绿部署（Blue-Green Deployment）

### 什么是蓝绿部署？

**生活类比：双车道**：

```
单车道（停机维护）：
    车道：施工
    → 所有车：停下 ❌
    → 交通瘫痪 ❌

双车道（蓝绿部署）：
    蓝车道：正常通行 ✅
    绿车道：施工
    → 绿车道修好了
    → 切换：蓝 → 绿
    → 蓝车道：施工
```

**软件蓝绿部署**：

```
初始状态：
    蓝环境：v1.0（100% 流量）
    绿环境：无

步骤 1：部署 v2.0 到绿环境
    蓝环境：v1.0（100% 流量）✅
    绿环境：v2.0（0% 流量）✅

步骤 2：测试绿环境
    测试：访问绿环境
    验证：v2.0 正常

步骤 3：切换流量
    蓝环境：v1.0（0% 流量）
    绿环境：v2.0（100% 流量）

步骤 4：验证
    如果 v2.0 有问题？
    → 立即切回蓝环境 ✅（秒级回滚）
```

---

### Kubernetes 蓝绿部署

**方式 1：使用 Service selector**：

```yaml
# v1.0 Deployment（蓝环境）
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fastapi
      version: "1.0"
  template:
    metadata:
      labels:
        app: fastapi
        version: "1.0"
    spec:
      containers:
      - name: fastapi
        image: fastapi-app:v1.0

---
# v2.0 Deployment（绿环境）
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-green
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fastapi
      version: "2.0"
  template:
    metadata:
      labels:
        app: fastapi
        version: "2.0"
    spec:
      containers:
      - name: fastapi
        image: fastapi-app:v2.0

---
# Service（指向蓝或绿）
apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  selector:
    app: fastapi
    version: "1.0"  # 切换到蓝环境
  ports:
  - port: 80
    targetPort: 8000
```

---

**切换流量**：

```bash
# 切换到绿环境（v2.0）
kubectl patch service fastapi-service -p '{"spec":{"selector":{"version":"2.0"}}}'

# 快速回滚到蓝环境（v1.0）
kubectl patch service fastapi-service -p '{"spec":{"selector":{"version":"1.0"}}}'
```

---

## 🐤 金丝雀发布（Canary Deployment）

### 什么是金丝雀发布？

**历史典故：矿工的金丝雀**：

```
矿井中：
    └─ 矿工带金丝雀下井
    └─ 如果金丝雀死亡？
    └─ 说明有毒气！矿工立即撤离 ✅

软件发布：
    └─ 先给少量用户用新版本
    └─ 如果有问题？
    └─ 立即回滚，影响范围小 ✅
```

---

### 金丝雀发布流程

```
初始状态：
    v1.0：100% 流量

步骤 1：部署 v2.0（1 个 Pod）
    v1.0：95% 流量（19 个 Pod）
    v2.0：5% 流量（1 个 Pod）

步骤 2：观察
    - 监控错误率
    - 监控性能指标
    - 查看用户反馈

步骤 3：逐步扩大
    v1.0：90% 流量
    v2.0：10% 流量

    v1.0：50% 流量
    v2.0：50% 流量

    v1.0：0% 流量
    v2.0：100% 流量

步骤 4：发现问题？
    → 立即回滚到 v1.0 ✅
```

---

### Kubernetes 金丝雀发布

**使用 Istio 实现金丝雀**：

```yaml
# v1.0 Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-v1
spec:
  replicas: 10
  selector:
    matchLabels:
      app: fastapi
      version: v1
  template:
    metadata:
      labels:
        app: fastapi
        version: v1
    spec:
      containers:
      - name: fastapi
        image: fastapi-app:v1.0

---
# v2.0 Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fastapi
      version: v2
  template:
    metadata:
      labels:
        app: fastapi
        version: v2
    spec:
      containers:
      - name: fastapi
        image: fastapi-app:v2.0

---
# VirtualService（Istio 流量分配）
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: fastapi-vs
spec:
  hosts:
  - fastapi-service
  http:
  - match:
    - headers:
        x-canary:
          exact: "true"
    route:
    - destination:
        host: fastapi-service
        subset: v2
  - route:
    # 95% 流量到 v1
    - destination:
        host: fastapi-service
        subset: v1
      weight: 95
    # 5% 流量到 v2
    - destination:
        host: fastapi-service
        subset: v2
      weight: 5
```

---

## 🎨 部署策略选择

### 何时使用哪种策略？

**停机部署**：
```
适用场景：
- 小项目
- 内部系统
- 非关键业务
- 用户很少

优点：
- 简单

缺点：
- 有停机时间
```

---

**滚动更新**：
```
适用场景：
- 生产环境（默认选择）
- 大多数应用
- 无需快速回滚

优点：
- 零停机
- 简单
- Kubernetes 原生支持

缺点：
- 回滚稍慢
```

---

**蓝绿部署**：
```
适用场景：
- 关键业务
- 需要快速回滚
- 有足够的资源

优点：
- 零停机
- 极快回滚
- 完整的环境切换

缺点：
- 需要 2 倍资源
- 配置复杂
```

---

**金丝雀发布**：
```
适用场景：
- 大规模应用
- 谨慎发布
- 需要逐步验证

优点：
- 零停机
- 风险最小
- 可以逐步扩大

缺点：
- 配置最复杂
- 需要流量管理工具（如 Istio）
- 发布周期长
```

---

## 🎯 小实验：自己动手

### 实验 1：Kubernetes 滚动更新

```bash
# 部署 v1.0
kubectl apply -f deployment-v1.yaml

# 更新到 v2.0（自动滚动更新）
kubectl set image deployment/fastapi-app fastapi=fastapi-app:v2.0

# 观察滚动更新过程
kubectl get pods -w

# 如果出现问题，回滚
kubectl rollout undo deployment/fastapi-app
```

---

### 实验 2：蓝绿部署

```bash
# 部署蓝环境（v1.0）
kubectl apply -f deployment-blue.yaml

# 部署绿环境（v2.0）
kubectl apply -f deployment-green.yaml

# 切换流量到绿环境
kubectl patch service fastapi-service -p '{"spec":{"selector":{"version":"2.0"}}}'

# 如果有问题，快速回滚
kubectl patch service fastapi-service -p '{"spec":{"selector":{"version":"1.0"}}}'
```

---

## 📚 检查理解

回答这些问题来测试你的理解：

1. **什么是滚动更新？**
   - 提示：逐步替换 Pod

2. **蓝绿部署的优点？**
   - 提示：快速回滚

3. **金丝雀发布的适用场景？**
   - 提示：大规模、谨慎发布

4. **如何实现零停机部署？**
   - 提示：新旧版本同时运行

5. **如何选择部署策略？**
   - 提示：根据业务需求、资源、风险

---

## 🚀 恭喜！

现在你已经完成了 Level 5 的所有学习内容！

**Level 5 总结**：
- ✅ Docker 容器化
- ✅ Docker Compose 多容器编排
- ✅ Kubernetes 编排
- ✅ CI/CD 自动化流程
- ✅ 多环境配置管理
- ✅ 部署策略（滚动更新、蓝绿部署、金丝雀发布）

**完整的学习路径**：
```
Level 1: 请求与响应
Level 2: 依赖注入
Level 3: 数据库与持久化
Level 4: 生产就绪
Level 5: 部署与运维 ✅

现在你已经能够：
- 从零构建生产级 FastAPI 应用
- 使用 Docker 和 Kubernetes 部署
- 实现 CI/CD 自动化
- 选择合适的部署策略
```

**祝你学习愉快！记住：自动化部署让应用稳定、可靠、快速迭代！** 🚀

---

**费曼技巧总结**：
- ✅ 换轮胎类比（滚动更新）
- ✅ 双车道类比（蓝绿部署）
- ✅ 矿井金丝雀类比（金丝雀发布）
- ✅ 完整的 K8s 部署配置
- ✅ 部署策略对比和选择
